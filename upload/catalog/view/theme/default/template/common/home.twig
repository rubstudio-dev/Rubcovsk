{{ header }}
<div id="common-home">
    <div id="page" class="">
        <div id="content">
            <div class="main-container">
                <div class="menu-block">
                    <div class="position-absolute m-2 z-1">
                        <a href="#menu" class="btn btn-light text-muted shadow-sm fs-4 rounded-circle m-1">
                            <ion-icon name="menu-outline"></ion-icon>
                        </a>
                        <a href="#menu" class="btn btn-light text-muted shadow-sm fs-4 rounded-circle m-1">
                            <ion-icon name="search"></ion-icon>
                        </a>
                        <a href="#menu" class="btn btn-light text-muted shadow-sm fs-4 rounded-circle m-1">
                            <ion-icon name="person-circle"></ion-icon>
                        </a>
                    </div>
                    <nav id="menu">
                        <ul>
                            {% if menu %}
                                {% for cat in menu %}
                                    <li>
                                        <span>{{ cat.name }}</span>
                                        <ul>
                                            {% if cat.child %}
                                                {% for child in cat.child %}
                                                    <li>
                                                        <a href="#">{{ child.name }}</a>
                                                        <span>{{ child.description }}</span>
                                                    </li>
                                                {% endfor %}
                                            {% else %}
                                                <div class="m-3">
                                                    <h5 class="text-muted text-center">Нету категорий! 😖</h5>
                                                </div>
                                            {% endif %}
                                        </ul>
                                    </li>
                                {% endfor %}
                            {% else %}
                                <div class="m-3">
                                    <h5 class="text-muted text-center">Нету родительских категорий! 😖</h5>
                                </div>
                            {% endif %}
                        </ul>
                    </nav>
                </div>
                <div id="map"></div>
            </div>
        </div>
    </div>
</div>
{{ footer }}
